# llmi Configuration Example
# Copy this to ~/.llmi and fill in your API key

# Required: Your Google Gemini API Key
GEMINI_API_KEY=your_api_key_here

# Optional: The model to use (default: models/gemini-2.5-flash-lite)
MODEL=models/gemini-2.5-flash-lite

# Optional: Number of history items to send for context (default: 100)
HIST_COMMANDS=100

# Optional: The prefix used to trigger translation (default: ;;)
TRIGGER_PREFIX=;;

# Optional: Path to a markdown file with custom rules/instructions
# CUSTOM_RULES_PATH=~/.llmi_rules.md
